export const appConfig = {
  selectors: {
    btn: 'MuiButton-root',
    btnLabel: 'MuiButton-label',
    formLabel: 'MuiFormLabel-root',
    formControlLabel: 'MuiFormControlLabel-root',
    inputContainer: 'MuiFormControl-root',
    input: 'input',
    error: 'Mui-error',
    modalContainer: 'MuiDialog-root',
    modalFormBlock: 'MuiDialogContent-root',
  },

  btnsText: {
    add: 'add',
    cancel: 'cancel',
  },

  regExpEmail: /^[A-Z0-9._%+-]+@[A-Z0-9-]+.+.[A-Z]{2,4}$/i,
  intervalCheckCloseModal: 100,
  intervalCheckOpenModal: 50,
};
